diff --git a/node_modules/expo-glass-tabs/build/glass-tab-bar.d.ts b/node_modules/expo-glass-tabs/build/glass-tab-bar.d.ts
index 1ff17b7..94725bb 100644
--- a/node_modules/expo-glass-tabs/build/glass-tab-bar.d.ts
+++ b/node_modules/expo-glass-tabs/build/glass-tab-bar.d.ts
@@ -25,6 +25,8 @@ export type GlassTabItem = {
 export type GlassTabBarProps = TabListProps & {
     /** Called when a tab is chosen by tap or scrub release. */
     onIndexSelected?: (index: number) => void;
+    /** Selected tab before the focused trigger effects synchronize. */
+    initialIndex?: number;
     theme?: Partial<GlassTabBarTheme>;
     /** Haptic tick while the scrub crosses tab boundaries (iOS). */
     haptics?: boolean;
diff --git a/node_modules/expo-glass-tabs/build/glass-tab-bar.js b/node_modules/expo-glass-tabs/build/glass-tab-bar.js
index 420cb04..3a3f92f 100644
--- a/node_modules/expo-glass-tabs/build/glass-tab-bar.js
+++ b/node_modules/expo-glass-tabs/build/glass-tab-bar.js
@@ -8,7 +8,6 @@ import { Gesture, GestureDetector } from 'react-native-gesture-handler';
 import Animated, { Extrapolation, interpolate, interpolateColor, runOnJS, useAnimatedStyle, useSharedValue, withSpring, } from 'react-native-reanimated';
 import { useSafeAreaInsets } from 'react-native-safe-area-context';
 import { setMinimized, useMinimizeState } from './minimize-context';
-import { ProgressiveBlur } from './progressive-blur';
 const AnimatedGlassView = Animated.createAnimatedComponent(GlassView);
 const EXPANDED_HEIGHT = 58;
 const MINIMIZED_HEIGHT = 44;
@@ -28,8 +27,6 @@ const ITEM_PAD_V = 7;
 /** Highlight content heights — radius must track h/2 for a true capsule. */
 const HIGHLIGHT_EXPANDED = ICON_SIZE + LABEL_BLOCK + ITEM_PAD_V * 2;
 const HIGHLIGHT_MINIMIZED = ICON_SIZE + ITEM_PAD_V * 2;
-/** How far the bottom progressive blur bleeds above the pill. */
-const BLUR_BLEED = 44;
 /**
  * Slide spring: interruptible by design — rapid tab-hopping retargets with
  * preserved velocity. Slight under-damping gives the pill a tiny settle,
@@ -49,15 +46,16 @@ const BarContext = createContext(null);
  * a sliding highlight, and finger scrubbing. Use via `TabList asChild`
  * with expo-router's headless tabs.
  */
-export function GlassTabBar({ children, onIndexSelected, theme: themeOverrides, haptics = true, ...props }) {
+export function GlassTabBar({ children, initialIndex = 0, onIndexSelected, theme: themeOverrides, haptics = true, ...props }) {
     const insets = useSafeAreaInsets();
     const { width: windowWidth } = useWindowDimensions();
     const minimized = useMinimizeState();
     const progress = minimized.progress;
-    const slideIndex = useSharedValue(0);
+    const tabCount = Math.max(Children.count(children), 1);
+    const clampedInitialIndex = Math.min(Math.max(initialIndex, 0), tabCount - 1);
+    const slideIndex = useSharedValue(clampedInitialIndex);
     const isDragging = useSharedValue(false);
     const lastTicked = useSharedValue(-1);
-    const tabCount = Math.max(Children.count(children), 1);
     const theme = useMemo(() => ({ ...DEFAULT_THEME, ...themeOverrides }), [themeOverrides]);
     // Picker-style tick while the highlight crosses tab boundaries mid-drag.
     const tick = useCallback(() => {
@@ -160,13 +158,7 @@ export function GlassTabBar({ children, onIndexSelected, theme: themeOverrides,
     });
     const bottomOffset = Math.max(insets.bottom - 16, 12);
     const barContext = useMemo(() => ({ slideIndex, isDragging, theme }), [slideIndex, isDragging, theme]);
-    return (_jsxs(View, { ...props, pointerEvents: "box-none", style: { position: 'absolute', left: 0, right: 0, bottom: 0 }, children: [_jsx(ProgressiveBlur, { direction: "bottom", style: {
-                    position: 'absolute',
-                    left: 0,
-                    right: 0,
-                    bottom: 0,
-                    height: bottomOffset + EXPANDED_HEIGHT + BLUR_BLEED,
-                } }), _jsx(View, { pointerEvents: "box-none", style: { marginHorizontal: BAR_MARGIN, marginBottom: bottomOffset }, children: _jsx(GestureDetector, { gesture: gesture, children: _jsxs(Animated.View, { style: barStyle, children: [isLiquidGlassAvailable() ? (_jsx(AnimatedGlassView, { glassEffectStyle: "regular", style: [
+    return (_jsxs(View, { ...props, pointerEvents: "box-none", style: { position: 'absolute', left: 0, right: 0, bottom: 0 }, children: [_jsx(View, { pointerEvents: "box-none", style: { marginHorizontal: BAR_MARGIN, marginBottom: bottomOffset }, children: _jsx(GestureDetector, { gesture: gesture, children: _jsxs(Animated.View, { style: barStyle, children: [isLiquidGlassAvailable() ? (_jsx(AnimatedGlassView, { glassEffectStyle: "regular", style: [
                                     StyleSheet.absoluteFill,
                                     { backgroundColor: theme.glassTint, borderCurve: 'continuous' },
                                     shapeStyle,
diff --git a/node_modules/expo-glass-tabs/build/index.d.ts b/node_modules/expo-glass-tabs/build/index.d.ts
index ab4a877..d3e69fe 100644
--- a/node_modules/expo-glass-tabs/build/index.d.ts
+++ b/node_modules/expo-glass-tabs/build/index.d.ts
@@ -1,5 +1,4 @@
 export { GlassTabBar, GlassTabButton, type GlassTabBarProps, type GlassTabBarTheme, type GlassTabItem, } from './glass-tab-bar';
 export { TabBarMinimizeProvider, useMinimizeOnScroll, useTabBarMinimized, MINIMIZE_SPRING, } from './minimize-context';
-export { ProgressiveBlur } from './progressive-blur';
 export { renderFadingTabScreen } from './fading-tab-slot';
 //# sourceMappingURL=index.d.ts.map
diff --git a/node_modules/expo-glass-tabs/build/index.js b/node_modules/expo-glass-tabs/build/index.js
index eb03d7f..40cf03f 100644
--- a/node_modules/expo-glass-tabs/build/index.js
+++ b/node_modules/expo-glass-tabs/build/index.js
@@ -1,4 +1,3 @@
 export { GlassTabBar, GlassTabButton, } from './glass-tab-bar';
 export { TabBarMinimizeProvider, useMinimizeOnScroll, useTabBarMinimized, MINIMIZE_SPRING, } from './minimize-context';
-export { ProgressiveBlur } from './progressive-blur';
 export { renderFadingTabScreen } from './fading-tab-slot';
diff --git a/node_modules/expo-glass-tabs/src/glass-tab-bar.tsx b/node_modules/expo-glass-tabs/src/glass-tab-bar.tsx
index c0f798b..ef94f1f 100644
--- a/node_modules/expo-glass-tabs/src/glass-tab-bar.tsx
+++ b/node_modules/expo-glass-tabs/src/glass-tab-bar.tsx
@@ -26,7 +26,6 @@ import { useSafeAreaInsets } from 'react-native-safe-area-context';
 import type { TabListProps, TabTriggerSlotProps } from 'expo-router/ui';
 
 import { MINIMIZE_SPRING, setMinimized, useMinimizeState } from './minimize-context';
-import { ProgressiveBlur } from './progressive-blur';
 
 const AnimatedGlassView = Animated.createAnimatedComponent(GlassView);
 
@@ -48,8 +47,6 @@ const ITEM_PAD_V = 7;
 /** Highlight content heights — radius must track h/2 for a true capsule. */
 const HIGHLIGHT_EXPANDED = ICON_SIZE + LABEL_BLOCK + ITEM_PAD_V * 2;
 const HIGHLIGHT_MINIMIZED = ICON_SIZE + ITEM_PAD_V * 2;
-/** How far the bottom progressive blur bleeds above the pill. */
-const BLUR_BLEED = 44;
 
 /**
  * Slide spring: interruptible by design — rapid tab-hopping retargets with
@@ -97,6 +94,8 @@ const BarContext = createContext<BarContextValue | null>(null);
 export type GlassTabBarProps = TabListProps & {
   /** Called when a tab is chosen by tap or scrub release. */
   onIndexSelected?: (index: number) => void;
+  /** Selected tab before the focused trigger effects synchronize. */
+  initialIndex?: number;
   theme?: Partial<GlassTabBarTheme>;
   /** Haptic tick while the scrub crosses tab boundaries (iOS). */
   haptics?: boolean;
@@ -109,6 +108,7 @@ export type GlassTabBarProps = TabListProps & {
  */
 export function GlassTabBar({
   children,
+  initialIndex = 0,
   onIndexSelected,
   theme: themeOverrides,
   haptics = true,
@@ -118,10 +118,14 @@ export function GlassTabBar({
   const { width: windowWidth } = useWindowDimensions();
   const minimized = useMinimizeState();
   const progress = minimized.progress;
-  const slideIndex = useSharedValue(0);
+  const tabCount = Math.max(Children.count(children), 1);
+  const clampedInitialIndex = Math.min(
+    Math.max(initialIndex, 0),
+    tabCount - 1,
+  );
+  const slideIndex = useSharedValue(clampedInitialIndex);
   const isDragging = useSharedValue(false);
   const lastTicked = useSharedValue(-1);
-  const tabCount = Math.max(Children.count(children), 1);
   const theme = useMemo(() => ({ ...DEFAULT_THEME, ...themeOverrides }), [themeOverrides]);
 
   // Picker-style tick while the highlight crosses tab boundaries mid-drag.
@@ -269,17 +273,6 @@ export function GlassTabBar({
       {...props}
       pointerEvents="box-none"
       style={{ position: 'absolute', left: 0, right: 0, bottom: 0 }}>
-      {/* Progressive blur rising from the screen's bottom edge behind the pill. */}
-      <ProgressiveBlur
-        direction="bottom"
-        style={{
-          position: 'absolute',
-          left: 0,
-          right: 0,
-          bottom: 0,
-          height: bottomOffset + EXPANDED_HEIGHT + BLUR_BLEED,
-        }}
-      />
       <View
         pointerEvents="box-none"
         style={{ marginHorizontal: BAR_MARGIN, marginBottom: bottomOffset }}>
diff --git a/node_modules/expo-glass-tabs/src/index.ts b/node_modules/expo-glass-tabs/src/index.ts
index 4c84b0a..9dea0e1 100644
--- a/node_modules/expo-glass-tabs/src/index.ts
+++ b/node_modules/expo-glass-tabs/src/index.ts
@@ -11,5 +11,4 @@ export {
   useTabBarMinimized,
   MINIMIZE_SPRING,
 } from './minimize-context';
-export { ProgressiveBlur } from './progressive-blur';
 export { renderFadingTabScreen } from './fading-tab-slot';
